{
    "name": "rennf93/laravel-guard",
    "description": "Laravel middleware adapter for guard-core-php: maps Illuminate HTTP requests to the guard-core engine and block verdicts back to Laravel responses",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.2",
        "rennf93/guard-core-php": "^4.0.4",
        "illuminate/http": "^10.0|^11.0|^12.0",
        "symfony/http-foundation": "^6.4|^7.0"
    },
    "autoload": {
        "psr-4": {
            "RenzoFranceschini\\GuardCoreLaravel\\": "src/"
        }
    },
    "scripts": {
        "test": "php bin/test_laravel.php",
        "lint": "for f in $(find src bin -name '*.php'); do php -l \"$f\" > /dev/null || exit 1; done && echo LINT_OK"
    },
    "repositories": [
        {
            "type": "path",
            "url": "../guard-core-php",
            "canonical": false
        },
        {
            "type": "vcs",
            "url": "https://github.com/rennf93/guard-core-php.git"
        }
    ],
    "config": {
        "platform": {
            "php": "8.2.0"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
